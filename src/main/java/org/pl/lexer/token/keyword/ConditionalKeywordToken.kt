package org.pl.lexer.token.keyword

import org.pl.lexer.token.IToken

/**
 * Identifies the conditional keyword "if".
 */
class ConditionalKeywordToken : IToken {
    override fun toString() = "ConditionalKeywordToken{if}"
}