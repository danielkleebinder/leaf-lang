package org.nyxlang.lexer.token.keyword

import org.nyxlang.lexer.token.IToken

/**
 * Identifies the "when" keyword.
 */
class WhenKeywordToken : IToken {
    override fun toString() = "WhenKeywordToken{when}"
}