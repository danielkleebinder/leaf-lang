package org.nyxlang.lexer.token.keyword

import org.nyxlang.lexer.token.IToken

/**
 * Identifies the "async" keyword.
 */
class AsyncKeywordToken : IToken {
    override fun toString() = "AsyncKeywordToken(async)"
}