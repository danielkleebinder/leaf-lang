package org.nyxlang.lexer.token.logical

import org.nyxlang.lexer.token.IToken

/**
 * Identifies the logical AND operator.
 */
class LogicalAndToken : IToken {
    override fun toString() = "AndToken{&&}"
}