package org.nyxlang.lexer.token.logical

import org.nyxlang.lexer.token.IToken

/**
 * Identifies the logical OR operator.
 */
class LogicalOrToken : IToken {
    override fun toString() = "LogicalOrToken{||}"
}