const add = fun (a: number, b: number) -> number = a + b
const sub = fun (a: number, b: number) -> number = a - b

fun compute(a: fun, b: fun) -> number {
  return a(5, 5) * b(10, 12)
}

// -20
const res = compute(add, sub)